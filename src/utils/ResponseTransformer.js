/*
on calling this js file, covert the parameter object to json and write json string to response object

*/
function writeResponse(objParams..., response, next{
  //object param to json convert
  //response.send();
  return next();
}
